# Requirements

This repository follows the staged requirements described in `pr1.txt`.

Current implementation target:
- Stage 4: S2A + CAW code generation.

Planned stages:
- Stage 5 through Stage 12 as defined in `pr1.txt`.
